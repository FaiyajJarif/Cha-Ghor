# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright demonstration suite (demo/). Artefacts only — the specs are tracked.
# demo/.auth holds real JWTs for the seeded accounts; it must never be committed.
demo/.auth/
demo-report/
test-results/
playwright-report/
blob-report/
