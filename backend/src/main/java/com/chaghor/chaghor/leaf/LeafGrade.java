package com.chaghor.chaghor.leaf;

// Matches the Postgres native enum `leaf_grade` ('A','B','C'). Uppercase labels
// on purpose so the Java name equals the DB label.
public enum LeafGrade {
    A,
    B,
    C
}
