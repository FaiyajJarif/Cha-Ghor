package com.chaghor.chaghor.sms;

// Matches the Postgres native enum `sms_category` ('payroll','loan','withdrawal','alert').
public enum SmsCategory {
    payroll,
    loan,
    withdrawal,
    alert
}
